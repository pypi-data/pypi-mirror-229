[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "maybe-type"
version = "1.0.3"
authors = [
  { name="Leander"},
]
description = "Addind the maybe type to python"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]