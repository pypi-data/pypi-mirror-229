from setuptools import setup, find_packages

setup(
    name='quantexa',
    version='0.1.4',
    packages=find_packages(include=['quantexa', 'quantexa.*']),
    description='This is the almighty quantexa package'
)
