[tool.poetry]
name = "datahub-api"
version = "0.1.0"
description = ""
authors = ["Haibo SUN <haibo.sun@liveramp.com>"]
readme = "README.md"
packages = [{include = "datahub_api"}]

[tool.poetry.dependencies]
python = "^3.8"

toml = "^0.10.2"
Jinja2 = "^3.1.2"
numpy = "^1.22.0"
retrying = "^1.3.4"
sqlglot = "^18.1.0"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
