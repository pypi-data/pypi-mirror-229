[tool.black]
line-length = 100
target-version = ['py37']
include = '\.pyi?$'
exclude = '''
(
    | .git
    | .venv
    | build
    | dist
)
'''



[tool.pytest.ini_options]
testpaths = ["tests/unit"]
env = '''
  DB1_CONNECTION = ""
  ANOTHER_ENV_VAR = "another_value"
'''