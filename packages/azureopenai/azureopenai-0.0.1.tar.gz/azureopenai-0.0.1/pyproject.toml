[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "azureopenai"
version = '0.0.1'
python_requires = ">=3.11"
dependencies = [
    'requests',
    'aiohttp',
    'jsonschema',
]
