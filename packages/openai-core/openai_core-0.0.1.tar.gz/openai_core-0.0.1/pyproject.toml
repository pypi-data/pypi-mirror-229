[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openai-core"
version = '0.0.1'
python_requires = ">=3.11"
dependencies = [
    'requests',
    'aiohttp',
    'jsonschema',
]
