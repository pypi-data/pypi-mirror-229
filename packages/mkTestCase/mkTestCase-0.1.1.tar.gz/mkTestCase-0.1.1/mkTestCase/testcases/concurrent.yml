Concurrent:
- name: 并发功能-双提交
  identifies: concurrent1
  step: 提交+提交：不同账号同时打开两个窗口，录入相同的数据提交，后一个提交的窗口会控制重复提交
  expect: 是
- name: 并发功能-删除修改
  identifies: concurrent2
  step: 删除+修改：不同账号同时打开两个窗口，对于同一笔数据，删除成功后，后一个修改的窗口控制不能修改成功
  expect: 是
- name: 并发功能-修改审批
  identifies: concurrent3
  step: 修改+审批：不同账号同时打开两个窗口，对于同一笔数据，修改成功后，审批提交时提示数据有改变并显示最新的修改数据后再让审批人提交
  expect: 是
- name: 并发功能-审批修改
  identifies: concurrent4
  step: 审批+修改：同一笔数据同时打开在两个窗口中，审批成功后，修改窗口控制不能修改成功
  expect: 是
- name: 并发功能-双审批
  identifies: concurrent5
  step: 审批+审批：同一笔数据同时打开在两个窗口中，审批成功后，后一个审批提交时控制不能提交成功
  expect: 是
- name: 并发功能-审批撤销
  identifies: concurrent6
  step: 审批+撤销：同一笔数据同时打开在两个窗口中，审批成功后，后一撤销提交时控制不能提交成功
  expect: 是
- name: 并发功能-审批打回通过
  identifies: concurrent7
  step: 审批打回+审批通过：同一笔数据同时打开在两个窗口中，审批成功后，后一审批打回控制不能打回成功
  expect: 是
- name: 并发功能-打回发送
  identifies: concurrent8
  step: 打回+发送：同一笔数据同时打开在两个窗口中，发送成功后，后一打回提交时控制不能打回成功
  expect: 是
