[tool.poetry]
name = "EventBus"
version = "0.1.0"
description = ""
authors = ["FEFU Analytics sector"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
asyncio = "^3.4.3"
pytest-asyncio = "^0.21.1"
pika = "^1.3.2"
aio-pika = "^9.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
