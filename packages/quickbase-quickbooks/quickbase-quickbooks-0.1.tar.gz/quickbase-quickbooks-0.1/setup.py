# setup.py

from setuptools import setup, find_packages

setup(
    name='quickbase-quickbooks',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # list packages your project depends on
    ],
)